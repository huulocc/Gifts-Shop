{
  "name": "giftshop-backend-prisma",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma schema and database tooling for the GiftShop backend.",
  "scripts": {
    "db:format": "prisma format",
    "db:validate": "prisma validate",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "prisma": "^6.0.0"
  }
}
